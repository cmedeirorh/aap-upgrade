---
- name: "Ensure work directory exists"
  ansible.builtin.file:
    path: "{{ install_path }}"
    state: directory
    mode: '0755'

- name: "Extract archive"
  ansible.builtin.unarchive:
    src: "{{ item }}"
    dest: "{{ install_path }}"
    remote_src: yes
  loop: "{{ download_files }}"
